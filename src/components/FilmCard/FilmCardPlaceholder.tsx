import { Skeleton } from '@mui/material';

function FilmCardPlaceholder() {
  return <Skeleton variant="rectangular" width="100%" height={447} sx={{ borderRadius: 4 }} />;
}

export default FilmCardPlaceholder;
