import { Skeleton } from '@mui/material';

function SpeciesCardPlaceholder() {
  return <Skeleton variant="rectangular" width="100%" height={247} sx={{ borderRadius: 4 }} />;
}

export default SpeciesCardPlaceholder;
