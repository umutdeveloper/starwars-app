import { Skeleton } from '@mui/material';

function PersonCardPlaceholder() {
  return <Skeleton variant="rectangular" width="100%" height={327} sx={{ borderRadius: 4 }} />;
}

export default PersonCardPlaceholder;
