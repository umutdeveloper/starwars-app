import { Skeleton } from '@mui/material';

function VehicleCardPlaceholder() {
  return <Skeleton variant="rectangular" width="100%" height={227} sx={{ borderRadius: 4 }} />;
}

export default VehicleCardPlaceholder;
